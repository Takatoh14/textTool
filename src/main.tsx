import './styles/index.scss';

import React from 'react';
import ReactDOM from 'react-dom/client';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <h1>Hello SCSS 🚀</h1>
    <p>SCSS が適用されていれば背景が黒、文字が水色になります。</p>
  </React.StrictMode>
);
